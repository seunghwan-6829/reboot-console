# 상세페이지 섹션 이미지 생성 파이프라인
> 엔진: Higgsfield MCP → `gpt_image_2_5` (GPT Image 2 계열 / OpenAI)
> 규격: **aspect_ratio `9:16` · resolution `2k` · quality `high`~`max`**
> 산출물: 웹 UI가 아니라 **실제 상품 상세페이지 이미지 타일** — 세로로 이어 붙이면 한 장의 상세페이지가 됨

---

## 1. 모델 확정 사항 (2026-09 기준 실측)

| 항목 | 값 |
|---|---|
| model id | `gpt_image_2_5` (카탈로그상 GPT Image 2 라인의 현행 버전, `gpt_image_2` 단독 ID는 없음) |
| aspect_ratio | `9:16` 지원 ✅ |
| resolution | `1k` / `2k` / `4k` → **`2k` 사용** |
| quality | low / medium / high / xhigh / max |
| variant | flare(기본) / sunburst |
| background | auto / opaque / transparent |
| 레퍼런스 입력 | `medias[].role = "image_references"` → **상품 실사 이미지를 넣어 제품 형태 일관성 유지** |

---

## 2. 섹션 아키텍처 (황금 구조 → 10타일 매핑)

> 각 타일 = 9:16 2K 이미지 1장. 세로 연결 시 자연스러운 흐름이 되도록 배경톤·서체 위계·여백 규칙을 전 타일 고정.

| # | 섹션 | 황금구조 | 트리거 | 이 타일에서 반드시 해야 할 일 |
|---|---|---|---|---|
| 01 | 메인 KV / 후킹 | 후킹 | 욕망+불안 | 헤드라인 1줄 + 상품 히어로컷. 1초 안에 멈추게. |
| 02 | 대세감 배지 | (후킹 직후 증거) | 신뢰 | 누적 판매량·평점·1위 배지. 짧게. |
| 03 | 문제 제기 | 문제 | 불안 | 고객 불편 **장면**을 비주얼로. 질문형 카피. |
| 04 | 해결 선언 / USP | 해결 | 욕망 | "그래서 준비했습니다" 전환 + Only/First/Best/Different 한 줄. |
| 05 | 핵심 혜택 ① | 해결 | 욕망 | Feature→Benefit 변환 1번. 디테일 매크로컷. |
| 06 | 핵심 혜택 ② | 해결 | 욕망 | 변환 2번. 사용 시나리오컷. |
| 07 | 스펙 / 사용법 | 해결 | 신뢰 | 기술용어를 혜택 언어로 번역한 스펙 카드. |
| 08 | 비교표 | 증거 | 신뢰 | 타사 vs 우리. 핵심 3~4축만. |
| 09 | 리얼 후기 / 인증 | 증거 | 신뢰 | 구체적 후기 + 인증마크. 흠집 효과 1개 섞기. |
| 10 | CTA / 보장 | CTA | 불안+신뢰 | 혜택 요약 + 긴박감 + 위험제거(환불보장) + 마이크로카피. |

**선택 추가 타일:** 구성/옵션 안내 · 배송/교환 정책 · 브랜드 스토리(자사몰 한정) · FAQ

---

## 3. 프롬프트 골격 (타일 1장당)

```
[역할] Korean e-commerce product detail page section tile, professional commercial photography + graphic design composite.
[캔버스] Vertical 9:16 poster-style tile, 2K. Full-bleed composition, safe margin 8% on all sides.
[아트디렉션] {무드 키워드} / {배경 컬러 코드} / {조명} / {카메라 앵글·렌즈감}
[주피사체] {상품 묘사 — 레퍼런스 이미지와 형태·색·로고 동일 유지}
[레이아웃] 상단 {헤드라인 영역} · 중앙 {비주얼} · 하단 {서브카피/배지 영역}
[타이포] {한국어 문구 — 3절 참고} / 굵은 산세리프 / 헤드라인:서브 = 2.5:1 위계
[금기] no website UI, no browser chrome, no navigation bar, no buttons that look like web widgets,
       no watermark, no lorem ipsum, no distorted hangul, no stock-photo border
```

### 전 타일 공통 고정값 (일관성 락)
생성 시작 전에 아래를 **먼저 확정**하고 10개 프롬프트에 그대로 복붙한다.
- 배경 컬러 팔레트 (메인 / 서브 / 포인트 3색 HEX)
- 조명 셋업 (예: soft diffused studio light, subtle top-left key)
- 카메라 톤 (예: 85mm, shallow depth of field, clean commercial)
- 서체 인상 (예: bold geometric sans, heavy weight)
- 여백 규칙 (safe margin 8%)
- 레퍼런스 이미지 = 매 호출 `medias[].role="image_references"`에 동일 상품컷 투입

---

## 4. 한글 텍스트 처리 전략 — 풀 베이킹 (2026-09-20 확정)

**결론: 헤드라인·본문·표·캡션까지 전부 이미지에 굽는다. 후공정 0.**

### 근거 (실측 3회)
| 회차 | 렌더 범위 | 결과 |
|---|---|---|
| v1 10장 | 짧은 헤드라인만 | 자소 깨짐 0건 |
| v2 6장 | 짧은 헤드라인만 | 자소 깨짐 0건 |
| v3 10장 | 헤드라인 + 본문 + 4행 표 + 비교 캡션 | **오타 2건 / 전체 약 60줄** |

v3의 오타는 자소 깨짐이 아니라 **유사 음절 치환**이었다.
- `양입니다` → `양입씨다` (니 → 씨)
- `물을 다시 섞음` → `물을 다시 섬음` (섞 → 섬)

둘 다 같은 프롬프트로 재생성하니 한 번에 교정됐다.

### 프롬프트 규칙
1. 렌더할 한국어를 **그대로** 프롬프트에 적는다. 의역 설명 금지.
2. 위치·크기·굵기·색을 줄 단위로 지정한다. (예: "직접 아래, 헤드라인의 1/4 크기, 레귤러, 진회색, 2줄")
3. 오타가 났던 어절은 다음 시도에서 **음절 단위로 못박는다.**
   > CRITICAL: the line must end with the syllables 양 / 입 / 니 / 다. Do not render it as 양입씨다.
4. 표는 4행 이내, 라벨은 2–3자. 그 이상이면 치환 오류율이 오른다.
5. 한 타일당 총 텍스트는 **6–8줄 이내**로 유지한다.

### 검수
생성 후 **한 글자씩 대조**한다. 눈으로 훑으면 `섞↔섬`, `니↔씨` 같은 치환을 놓친다.
불합격 타일은 3번 규칙을 적용해 재생성. 타일당 3크레딧이므로 재생성 비용은 무시해도 된다.

### 폐기된 전략
- ~~하이브리드(헤드라인만 굽고 본문은 Figma/PS)~~ — 사용자가 후공정을 원하지 않아 폐기.
- ~~비주얼 온리~~ — 동일 이유로 폐기.

---

## 5. 실행 절차 (확정 순서)

> 핵심: **이미지 가공은 기획 뒤에 한다.** 스토리라인이 나와야 어떤 컷이 필요한지 정해지고,
> 그래야 필요한 가공만 한다. 먼저 가공하면 안 쓰는 이미지가 나온다.

```
① 상품 폴더 준비          사용자: 폴더 생성 → 상품 원본 사진 투입
② 브리프 작성             사용자: 브리프_열기.bat → 폼 작성 → 저장하기
                        → 같은 폴더에 brief.json 생성
③ 원본 확인              Claude: 사진 검토 (형태·라벨·촬영 상태·사용 가능 컷 판정)
④ 카피 + 스토리라인       Claude: brief.json + 플레이북 기준으로 10섹션 기획안 작성
                        → 사용자 승인
⑤ 필요 컷 도출            Claude: 승인된 스토리라인에서 "어떤 제품컷이 몇 개 필요한지" 확정
⑥ 상품 이미지 가공         Claude: 7절 TRACK A — 누끼 → 업스케일 → 배경 합성
                        → 가공본 검수 (비율 오차 0.5% 이내)
⑦ 섹션 타일 제작          Claude: 가공본 + gpt_image_2_5 로 10타일 생성
⑧ 검수 & 재생성           7.4 검수 게이트 통과할 때까지
⑨ 합본                   세로 연결 (sandbox_exec + ImageMagick)
```

**도구 호출 순서**
- ③ 원본 업로드: `media_upload_widget` (그 턴의 유일한 도구여야 함)
- ⑥ 가공: `remove_background` → `upscale_image` → `sandbox_exec`
- ⑦ 타일: `generate_image_batch` (최대 12건 병렬, index = 섹션번호)
  → `jobs_wait` → `show_generation_by_ids` 1회

**비용**
- 생성 전 `get_cost: true` 로 프리플라이트 후 사용자에게 보고하고 진행

## 6. 절대 금지 (디자인)
- 웹페이지 UI(네비게이션 바, 브라우저 창, 카드형 웹 컴포넌트) 형태로 나오는 것
- 랜딩페이지 목업처럼 보이는 것
- 가로형 배너 구도
- → **목표는 "쿠팡/스마트스토어에 그대로 올라가는 세로 상세페이지 이미지"**

---

# 7. 상품 이미지 가공 파이프라인 (무왜곡 보장)

> 요구사항: 촬영본을 던지면 가공 이미지를 만들고, 그걸로 섹션 타일을 만든다.
> **절대 조건: 제품이 깨지거나 비율이 이상해지면 안 된다.**

## 7.1 왜 "AI에 그냥 맡기면" 제품이 망가지는가

`gpt_image_2_5`에 촬영본을 `image_references`로 넣고 "이 제품으로 상세페이지 만들어줘"라고 하면,
모델은 **참조할 뿐 제품을 다시 그린다.** 그 결과:

- 라벨 문구·로고가 뭉개지거나 다른 글자로 바뀜
- 병목·뚜껑 비율이 미세하게 달라짐
- 색상이 톤 시프트됨
- 형태가 "비슷하지만 다른 제품"이 됨

→ 그래서 **제품 픽셀은 절대 재생성하지 않는 경로**로 간다.

## 7.2 2트랙 전략

### 🔒 TRACK A — 제품 보존 합성 (기본값, 제품이 주인공인 타일)

제품 픽셀을 원본 그대로 유지하고, 배경만 AI로 만든다.

```
원본 촬영본
  └─ ① media_upload_widget          업로드
  └─ ② remove_background            누끼 — 원본 픽셀 그대로 잘라냄 (재생성 아님)
  └─ ③ upscale_image (2k/4k)        기하 구조 보존 업스케일
        ↓
배경판  gpt_image_2_5 (9:16, 2k)     제품 없이 "빈 배경/스튜디오 씬"만 생성
        ↓
  └─ ④ sandbox_exec + ImageMagick   비율 고정 합성 (아래 7.3 규칙)
```

**보장 근거**
- `remove_background`는 프롬프트를 받지 않는다. 생성 도구가 아니라 마스킹 도구다.
- `upscale_image`도 프롬프트가 없다. 해상도만 올린다.
- 합성 단계에서 `-resize` 시 **비율 유지 플래그만 사용**하고 강제 리사이즈(`!`)는 절대 쓰지 않는다.
- 결과적으로 제품 영역은 원본의 비례를 100% 유지한다.

### 🎨 TRACK B — 풀 생성 (제품이 주인공이 아닌 타일)

배경 씬, 분위기컷, 문제 제기 장면, 패턴 배경 등 **제품 형태가 중요하지 않은 타일**만 `gpt_image_2_5`로 통째 생성.

**타일별 트랙 배정**

| # | 섹션 | 트랙 | 이유 |
|---|---|---|---|
| 01 | 메인 KV | **A** | 제품 히어로컷 — 형태 정확도가 생명 |
| 02 | 대세감 배지 | B | 그래픽 위주 |
| 03 | 문제 제기 | B | 상황 연출컷, 제품 없음/작게 |
| 04 | 해결 선언 | **A** | 제품 등장 |
| 05 | 핵심 혜택 ① | **A** | 디테일 매크로컷 |
| 06 | 핵심 혜택 ② | **A** | 사용 시나리오 |
| 07 | 스펙/사용법 | **A** | 제품 정면 기준컷 |
| 08 | 비교표 | B | 도식 위주 |
| 09 | 후기/인증 | B | 그래픽 위주 |
| 10 | CTA | **A** | 제품 마무리컷 |

## 7.3 합성 규칙 (ImageMagick)

```bash
# 배경판: 9:16 2K = 1152 x 2048
# 제품 누끼를 "긴 변 기준 비율 유지"로만 축소 — '!' 절대 금지
magick bg.png \
  \( product_cutout.png -resize 900x1200 \) \
  -gravity center -geometry +0-120 -composite \
  out_tile.png
```

**불변 규칙**
1. `-resize WxH` 는 **박스 안에 맞추는** 동작이다. 반드시 이 형태만 쓴다.
2. `-resize WxH!` (느낌표) — **금지.** 강제로 늘려 비율을 깬다.
3. `-distort`, `-shear`, `-liquid-rescale` — **금지.**
4. 제품 회전은 촬영 각도 보정이 명백히 필요할 때만, 5도 이내.
5. 합성 후 **원본 대비 가로세로비 검산**:
   `원본 W/H` 와 `배치된 W/H` 차이가 0.5% 이내여야 통과.

## 7.4 검수 게이트 (매 타일)

| 체크 | 불합격 시 |
|---|---|
| 제품 가로세로비 원본과 일치 (오차 0.5% 이내) | 합성 다시 |
| 라벨 문구·로고 판독 가능하고 원본과 동일 | 누끼 소스 재선택 |
| 누끼 경계에 배경색 잔상(프린지) 없음 | 원본 재촬영본으로 교체 |
| 제품 그림자 방향 = 배경판 조명 방향 | 배경판 재생성 |
| 한글 헤드라인 자소 안 깨짐 | 해당 타일 재생성 |
| 타일 간 배경톤·서체 위계 연속성 | 아트디렉션 재고정 |

## 7.5 촬영본 요구사항 (사용자에게)

대충 던져도 되지만, 아래를 지키면 결과가 확연히 좋아진다.

- **초점이 제품에 맞아 있을 것** (흔들린 사진은 업스케일로도 복구 안 됨)
- **제품 전체가 프레임 안에 들어올 것** (잘린 부분은 만들어낼 수 없음)
- **배경과 제품 색이 너무 비슷하지 않을 것** (누끼 경계 품질 좌우)
- **라벨/로고가 읽히는 컷 1장 이상** (정면 기준컷용)
- 각도별 2~4장 있으면 타일마다 다른 컷을 쓸 수 있어 좋음
- 조명은 상관없음 — 배경을 새로 깔기 때문

---

# 8. 완성 타일 편집 (2026-09-20 검증)

**결론: 된다.** 완성된 타일을 `image_references`로 다시 넣고 "이것만 바꿔라"라고 지시하면 나머지를 유지한 채 수정된다.

## 절차
```
1) media_upload           수정할 타일(tiles/NN.png)
2) curl PUT → media_confirm
3) generate_image
     medias: [{role:"image_references", value:<그 media_id>}]
     prompt: "Edit the supplied image. Keep EVERYTHING else identical … MAKE EXACTLY N CHANGES: …"
```
비용 **3크레딧/회**. 처음부터 다시 뽑는 것과 같은 값이지만 구도가 유지된다.

## 프롬프트 골격
```
Edit the supplied image. Keep EVERYTHING else pixel-for-pixel identical —
same background and texture, same composition, same product and its label,
same lighting and shadow, same margins and positions.

MAKE EXACTLY TWO CHANGES:
1) <바꿀 것 1 — 유지할 속성을 같이 명시>
   "Keep the identical gold color, gradient, weight, size and position."
2) <바꿀 것 2>

Do NOT change <건드리면 안 되는 요소를 이름으로 나열>.
```

**핵심: "무엇을 바꿀지"보다 "무엇을 유지할지"를 더 길게 쓴다.**

## 1차 실측 (타일 01)
| 요청 | 결과 |
|---|---|
| 골드 `100%` → `130%` | ✅ 같은 골드·메탈 그라데이션·크기·위치로 교체 |
| 상단 배지 서체를 프리텐다드풍 고딕으로 | ✅ 획 굵기 균일·기하학적 형태로 바뀜 |
| 흰 알약 서브카피 유지 | ✅ 그대로 |
| 파우치 라벨·150 mL·※캡션 유지 | ✅ 그대로 |
| 배경 버건디·물결 텍스처 유지 | ✅ 그대로 |

## 한계 — 픽셀 단위 편집이 아니다
모델이 **다시 그린다.** 지정하지 않은 부분도 미세하게 움직인다.
1차 실측에서 관찰된 드리프트:
- 제품 위치가 소폭 위로 이동, 크기 약간 커짐
- 파우치 표면 반사광 패턴이 달라짐
- 헤드라인 행간이 약간 좁아짐

## 잘 되는 편집 / 안 되는 편집
| | |
|---|---|
| ✅ 잘 됨 | 숫자·문구 교체, 서체 인상 변경, 색 변경, 강조 스타일 변경, 배지/라벨 텍스트 교체 |
| ⚠️ 드리프트 | 제품 위치·크기, 조명, 그림자, 반사광 |
| ❌ 안 됨 | "여기 5px만 옮겨줘" 같은 좌표 단위 조정, 원본과 100% 동일한 배경 보존 |

**같은 타일을 여러 번 연쇄 편집하면 드리프트가 누적된다.**
수정이 3개 이상 쌓이면 원래 생성 프롬프트를 고쳐서 새로 뽑는 편이 낫다.

## 운용 규칙
- 편집본은 `edits/` 에 저장하고 원본 `tiles/` 는 건드리지 않는다.
- 확정되면 `tiles/` 로 승격하고 이전 버전은 `tiles/vN/` 로 보관.

---

# 9. 섹션 연장 (2026-09-20 검증)

**결론: 된다.** 9:16이 생성 가능한 가장 긴 비율이므로, 한 섹션을 길게 만들려면 **타일을 이어 붙인다.**
상세페이지는 어차피 세로 한 장으로 합쳐지므로 타일 개수는 자유다.

## 이음새를 없애는 프롬프트 문구
```
CONTINUATION TILE: this image will be stacked DIRECTLY BENEATH another tile
with the identical background. The background must bleed to all four edges
with NO border, NO frame, NO margin band and NO vignette at the top or bottom
edge, so the seam between the two tiles is invisible.

BACKGROUND: <앞 타일과 완전히 동일한 배경 서술>, perfectly even from the
very top edge to the very bottom edge.
```
+ 세이프마진을 `8% safe margin on the left and right only` 로 바꾼다. 상하 여백을 두면 띠가 생긴다.

## 이음새 검산 (PIL)
맞닿는 두 변의 4px 평균색을 비교한다.
```python
def edge(im, top):
    box = (0,0,im.width,4) if top else (0,im.height-4,im.width,im.height)
    px = list(im.crop(box).convert("RGB").getdata()); n=len(px)
    return tuple(round(sum(c[k] for c in px)/n) for k in range(3))
```
| RGB 최대편차 | 판정 |
|---|---|
| ≤ 12 | 안 보임 — 그대로 합본 |
| 13~25 | 약간 보임 — 경계에 20px 그라데이션 블렌드 |
| > 25 | 보정 필요 — 배경 서술을 맞춰 재생성 |

**1차 실측: 편차 8 / 12 → 둘 다 안 보임.**

## 끊는 위치
- **단색·텍스처 배경끼리** 맞닿게 끊는다. 이음새가 사라진다.
- **사진 타일은 위쪽을 단색으로 시작**하게 만든다. 사진끼리 맞물리면 경계가 드러난다.
- 배경색이 바뀌는 지점(버건디→크림)은 오히려 **섹션 구분**이 되므로 그대로 둔다.

## ⚠️ 주의 — 분량을 늘리면 없는 사실이 늘어난다
이번 연장에서 `다섯 시간 안에`, `당일 입고`, `세 번 헹궈`, `저온`은 **전부 지어낸 공정**이다.
섹션을 길게 만들수록 채울 내용이 필요해지고, 그 자리를 추측이 메운다.
**연장 전에 실제 공정·수치를 먼저 확보한다.** 없으면 타일을 늘리지 않는다.

## 이번에 나온 내 실수
프롬프트에 `씻어`를 `씨어`로 잘못 적어 그대로 렌더됐다. 모델 오류가 아니라 **입력 오타**.
→ 프롬프트에 넣는 한국어는 **보내기 전에 한 번 더 읽는다.**
→ 받침이 까다로운 단어(`씻다`)는 애초에 쉬운 말로 바꾼다. `세 번 헹궈` 로 교체하니 한 번에 통과.

---

# 10. 검수 콘솔 (2026-09-20)

프로젝트 폴더의 `미리보기.html` 에 **검수 탭**이 붙어 있다. 3탭 구조.

| 탭 | 용도 |
|---|---|
| 이어붙이기 | 실제 상세페이지처럼 세로로 이어 붙여 흐름 확인. 폭 320~720px 조절 |
| 그리드 | 전체를 한눈에. 클릭하면 확대 |
| **검수** | 한 장씩 판정하고 요청을 남김 |

## 검수 탭 구조
```
[썸네일 레일]   [타일 확대]   [판정 패널]
 상태 점 표시                 판정 4종 · 좋았던 점 · 수정 요청
 클릭 시 이동                 빠른 요청 칩 10개 · 연장 0~3장 + 메모
```

**판정 4종** — 승인 / 수정 / 연장 / 보류.
승인·보류는 **자동으로 다음 장**으로 넘어간다. 12장을 빠르게 훑는 것이 목적.

**단축키** — `←` `→` 이동 · `1`~`4` 판정 · `Enter` 다음 · `Esc` 닫기

**연장** — `+1`~`+3` 을 고르면 판정이 자동으로 '연장'이 되고, 메모에 적은 내용이 9절의 섹션 연장 작업 지시가 된다.

## 산출물
하단 **지시서 만들기** → 사람이 읽는 작업 지시서 생성.
- **지시서 복사** → 클립보드 → 대화창에 붙여넣기 (가장 빠름)
- **JSON 저장** → `review.json` (FSA 지원 브라우저는 폴더에 직접, 아니면 다운로드)

지시서 형식:
```
■ 그대로 확정 (N장)
   01, 05, 07
■ 작업 요청 (N건)
▸ 02. 원재료  [수정]
   유지: 골드 숫자 강조가 잘 보임
   수정: 형광 하이라이트 색을 좀 더 연하게
▸ 03. 문제  [연장]
   연장: 이 섹션 뒤에 2장 추가
         └ 실제 공정 사진
■ 보류
■ 예상 작업량
   재생성·편집 2장 + 신규 2장 = 약 12 크레딧
```

`유지:` 항목은 재작업 시 **그대로 지켜야 할 속성**으로 프롬프트에 넣는다 (8절의 편집 원칙과 동일).

## 운용
- 검수 내용은 localStorage에 자동 저장된다. 브라우저를 닫아도 남는다.
- 새 프로젝트에 재사용하려면 `미리보기.html` 상단의 `PROJECT` 와 `TILES` 배열만 갈아끼운다.
- 타일이 늘거나 줄면 `TILES` 를 갱신해야 검수 탭에 반영된다.

---

# 11. 섹션별 비율 규칙 + 클라이언트 프리뷰 (2026-09-22)

## 비율
`gpt_image_2_5` 는 `9:16` `3:4` `16:9` 등을 지원한다. 섹션 성격에 맞춰 섞는다.

| 비율 | 출력 | 쓰는 섹션 |
|---|---|---|
| **16:9** | 2688×1520 | **배송 안내 — 페이지 최상단 배너** |
| **9:16** | 1520×2688 | 인트로·구성·문제·해결·포인트·활용·비교·정보·CTA (기본) |
| **3:4** | 1744×2336 | **Q&A · 추천 · 교환반품** — 텍스트 위주라 세로 여백이 남는 섹션 |

프롬프트는 동일 문법, `aspect_ratio` 만 바꾼다. 3:4는 카드가 폭을 채우도록 "filling the width" 를 넣는다.
`tiles/manifest.json` 에 `ratio` 를 기록한다. 이어붙일 때는 폭 기준으로 쌓이므로 비율이 섞여도 실제 상세페이지와 같다.

## 파일 순서 = 페이지 순서
`tiles/01.png` 부터 번호순. 배송이 최상단이면 배송이 `01`. 재번호는 **내림차순**으로(덮어쓰기 방지), 교체된 구버전은 `tiles/vX/` 에 보관.

## 클라이언트 프리뷰 내보내기
콘솔 → 타일 → **클라이언트 프리뷰** → `export/<상품>_시안_<버전>.html` 한 파일.
- 타일을 JPEG(q82)로 **base64 임베드**. 리샘플 없음(원본 픽셀 무변경), 15장 ≈ 7MB.
- 받는 사람은 더블클릭으로 열어 **모바일 390 / 태블릿 768 / 기본 860** 폭 전환, 배율 35~100%, 번호 표시, **장별 피드백 입력 → "피드백 전체 복사"** 로 회신.
- 피드백은 받는 사람 브라우저 localStorage 에만 저장된다(우리 쪽으로 자동 전송 없음). 복사해서 보내야 한다.
- 서버 엔드포인트 `GET /local/export?name=&ver=` (`_launch.py build_export`).

## 콘솔 프리뷰(이어붙이기)
기준 폭 **860px**, 배율은 CSS 폭만 조절. 원본 픽셀 무변경.
스크롤 불가 원인은 `.shell` 100vh 안에서 `.main` 이 내용만큼 늘어나 `body overflow:hidden` 에 갇힌 것 → `grid-template-rows:minmax(0,100vh)` + `.main/.view{min-height:0}` 로 해결.

## 함정 — 8777 이중 바인딩 (2026-09-22 해결)
증상: 서버를 새로 띄워도 구코드가 응답(새 엔드포인트 404), F5 하면 "사이트에 연결할 수 없음".
원인: Python `HTTPServer.allow_reuse_address = 1`(SO_REUSEADDR) 이 Windows 에서는 **이미 LISTEN 중인 포트에 두 번째 바인딩을 허용**한다. "이미 실행 중" 분기는 한 번도 타지 않았고 리스너가 둘이 되어 요청이 옛 프로세스로 갔다.
해결: `allow_reuse_address = False` → 바인딩 실패(OSError) → `/local/ping` 으로 우리 서버인지 확인 → `POST /local/shutdown` → 0.2s 간격 30회 재바인딩. 마지막에 실행한 런처가 항상 살아남는다. 검증: A 실행 → B 실행 → A 정상 종료(exit 0), B 단독 LISTEN.

## 콘솔 v3 (2026-09-22) — 화면 구조
- **홈** = 대시보드(스테퍼 5단계 · 현황 · 타일 미리보기). **프로젝트** = 폴더 카드(ctime · mtime · 타일 첫/마지막 생성시각 차 = 제작 소요 · export 수, `/local/projects` 가 계산).
- **사진 분석은 브리프 0단계 카드**로 흡수. `SECTIONS[0]=photos`, 왼쪽 메뉴가 진행 스테퍼(완료 체크 · 작성 중 표시). `App.curSec` 로 현재 카드 추적.
- **타일 스크롤 스파이**: IntersectionObserver 는 타일이 뷰포트보다 커서 ratio 임계값에 못 미침 → 뷰 상단 30% 지점을 지나는 `.tl` 을 scroll 이벤트(80ms 타이머)로 계산. `requestAnimationFrame` 은 숨은 탭에서 멈추므로 쓰지 않음. `App.curTile` → `renderSide()`.
- 검수 ↔ 타일 동기화: 상태는 `App.review[n]` 하나. 타일 뷰가 배지(`.rb`)로 그리고 사이드바 점 색으로 표시. 검수에서 판정하면 즉시 반영.
- 여백: 폼 화면(`.wrap`)은 왼쪽 정렬(`margin:0`), 대지 화면(타일 `.wrap.center` · 검수 `.rv-stage`)은 중앙. 사용자 요청.
- 애니메이션: 뷰 진입 `vin`, 카드 스태거(`animation-delay`), 완료 번호 `pop`, 필수 누락 `shake`, 스테퍼 현재 단계 `pulse`, 호버 리프트. `prefers-reduced-motion` 이면 전부 끔.
- 토스트: 왼쪽 아래(`left: rail+16px`), `align-items:flex-end` 로 오른쪽 정렬, 최대 3개, 로딩 중간 토스트 제거.

## 콘솔 v4 (2026-09-23) — EXE 전환
- `desktop/main.js` 가 `_launch.py` 와 같은 `/local/*` API 를 Node 로 내장. 정적 파일은 EXE 안의 `app/` 에서, 프로젝트 파일은 ROOT 에서 서빙. 포트는 임의(127.0.0.1). 경로 이탈은 403.
- 내보내기 템플릿을 `app/export_template.html` 로 분리해 파이썬·Node 공용. Node 는 `nativeImage.toJPEG(82)` 로 인코딩(리샘플 없음) — 15장 6.9MB 로 파이썬과 동일.
- **설정 → 연결**: `/local/tools` GET 이 `claude --version`·`codex --version`·`~/.claude.json` 의 mcpServers.higgsfield 로 상태 판정. POST `do=` 로 설치(npm i -g)·로그인(`claude`/`codex login`)·MCP 등록(`claude mcp add --transport http --scope user higgsfield …`)·`run-claude`(터미널에서 `claude "<폴더> 만들어줘"`)·루트 변경. 터미널은 userData/run/*.cmd 를 UTF-8(chcp 65001) 로 써서 `start` 로 띄움 — 한글 폴더명·프롬프트 안전.
- 브라우저 확대 차단: document `wheel`(ctrl) preventDefault + Ctrl+±/0 가로채기, Electron `setVisualZoomLevelLimits(1,1)` + 메뉴 제거.
- 타일 배율: `Tiles.zoomAt(z, anchorY)` — 페이지 상단 오프셋과 커서의 페이지 내 비율로 `scrollTop` 을 재계산해 커서 아래 지점을 고정. `.strip .pg` 의 width transition 은 이 계산을 깨서 제거.
- 검수 뷰어: `.rv-can` 을 스테이지 중앙 기준 `translate(x,y) scale(s)`. 휠은 커서 기준(`x = px - (px - x) * k`), 드래그 pan, 더블클릭 fit↔100%, ResizeObserver 로 fit 재계산. 라이트박스 제거.
- 시작 화면: `sessionStorage reboot:booted` 없으면(새 창) 홈, 있으면(F5) 마지막 화면.
- 제거한 것: FSA(`showDirectoryPicker`)·`fallbackDir`·IndexedDB 핸들 복원 — 서버가 항상 있으므로 불필요. 남긴 것: `_launch.py`(EXE 없는 PC), 클라우드(Vercel) 메뉴, 레거시 `상세페이지_브리프.html`·프로젝트별 `미리보기.html`(독립 파일이라 해가 없음).

## 콘솔 v4.1 (2026-09-23) — 창 없는 로그인 · 헤드리스 제작 · 영역 검수
- **로그인 딸깍**: `claude auth login`(브라우저 OAuth, 로컬 콜백) · `claude mcp login higgsfield` · `codex login` 을 EXE 가 `windowsHide` 로 숨겨 실행. 상태는 `claude auth status --json`(loggedIn/email) · `~/.claude/.credentials.json` 의 `mcpOAuth["higgsfield|…"]` · `codex login status`(stderr 포함) 로 판정, 앱이 3초 폴링. 설치(npm i -g)만 터미널.
- **헤드리스 제작**: `POST /local/run {name, mode:make|revise|custom}` → `claude -p` stream-json 을 줄 단위로 파싱(assistant text / tool_use 이름+요약 / result 비용·턴). `GET /local/run?since=N` 폴링, `do=stop` 은 `taskkill /T`. 창 닫을 때 실행 중이면 확인. 동시 1건.
- **영역 검수**: `.rv-can{--s}` 위 `.rv-ann` 오버레이, 좌표는 이미지 기준 0~1 비율. 박스 테두리·번호는 `calc(px / var(--s))` 로 배율 무관. 저장은 localStorage + 0.9초 디바운스 `review.json`. `buildMarked()` 가 canvas 로 빨간 번호 박스를 그려 `review/NN_marked.png` 저장(`/local/save-image`, PNG 매직 검사, review/ 만 허용).
- **순서 변경**: 사이드바 HTML5 DnD → `App.tiles` 재배열 → `tiles/manifest.json._order`. 파이썬·Node 내보내기 모두 `_order` 우선.
- 검수 데이터 구조 변경: `{status,good,request,ext}` → `{regions:[{x,y,w,h,text}], note}`. `migrateReview()` 가 구형 request/extNote 를 note 로 합침.

## 설치형 + 자동 업데이트 (2026-09-23)
- 대상: NSIS 설치형(`reboot-console-setup-<v>.exe`). 포터블은 자동 업데이트 없음(앱이 안내).
- 흐름: `UPD.init()`(켤 때·6시간) → electron-updater 가 GitHub Releases `latest.yml` 확인 → 앱 `Update.poll`(4초 후·10분마다) → `state=available` 이면 **정중앙 모달**(dismissable:false) → 지금 업데이트 → `download`(진행 토스트) → `downloaded` → "다시 시작하여 설치" → `quitAndInstall`. AI 작업 중이면 설치 알림을 미룸.
- 배포 함정: electron-builder 가 `releaseType:"release"` 로 릴리스를 만들면 GitHub 가 "valid tag" 422 를 내고 `latest.yml` 이 빠질 수 있다 → `releaseType:"draft"` + 빌드 후 `gh release edit vX --draft=false --latest`(npm run release 에 포함). yml 이 빠지면 `gh release upload vX dist/latest.yml dist/*.blockmap --clobber`.
- 설치형 루트: Program Files 안이라 `looksLikeRoot()`(YYMMDD_ 폴더 존재) 를 못 넘기면 첫 실행에 폴더 선택 → config.json.
- 검증: 4.1.1 설치본 --smoke(격리 userData) 에서 available 4.1.2 → download 진행 확인. `--smoke` 는 `%TEMP%\reboot-smoke-data` 를 userData 로 써서 실행 중인 콘솔과 잠금 충돌 없음(config.json 은 UTF-8 로 써야 함 — bash printf 는 한글을 깨뜨림).

## 콘솔 v4.2 (2026-09-23) — 사진을 앱 안에서
- `POST /local/project-create?name=` (이름 정제, 있으면 그대로 열기) · `POST /local/photo?name&file=` (바이너리, 이미지 확장자만, 중복은 ` (n)`) · `POST /local/photo-delete` (삭제 대신 `_trash/` 로 이동). 파이썬·Node 동일.
- 앱: `FS.addPhotos(files)` 가 파일선택·드롭(브리프 뷰 전체)·`paste`(입력칸 밖) 를 한 경로로 받는다. 클립보드 이미지는 `붙여넣기_<시각>.png`. 추가/삭제 후 `FS.load` 재분석.
- UI 에서 뺀 것: 사이드바 `brief.json 저장`·`지시서 보기`, 브리프 액션바 저장/요약, 제작 대화상자의 지시서 복사. 파일은 내부적으로 계속 씀(`brief.json` 은 섹션 적용 시 조용히, `order.json` 은 제작 시). 웹(파이썬) 모드만 "명령 복사" 유지.

## 콘솔 v4.1 (2026-09-23) — 창 없는 로그인 · 헤드리스 제작 · 영역 검수
- **로그인 딸깍**: `claude auth login`(브라우저 OAuth, 로컬 콜백) · `claude mcp login higgsfield` · `codex login` 을 EXE 가 `windowsHide` 로 숨겨 실행. 상태는 `claude auth status --json`(loggedIn/email) · `~/.claude/.credentials.json` 의 `mcpOAuth["higgsfield|…"]` · `codex login status`(stderr 포함) 로 판정, 앱이 3초 폴링. 설치(npm i -g)만 터미널.
- **헤드리스 제작**: `POST /local/run {name, mode:make|revise|custom}` → `claude -p` stream-json 을 줄 단위로 파싱(assistant text / tool_use 이름+요약 / result 비용·턴). `GET /local/run?since=N` 폴링, `do=stop` 은 `taskkill /T`. 창 닫을 때 실행 중이면 확인. 동시 1건.
- **영역 검수**: `.rv-can{--s}` 위 `.rv-ann` 오버레이, 좌표는 이미지 기준 0~1 비율. 박스 테두리·번호는 `calc(px / var(--s))` 로 배율 무관. 저장은 localStorage + 0.9초 디바운스 `review.json`. `buildMarked()` 가 canvas 로 빨간 번호 박스를 그려 `review/NN_marked.png` 저장(`/local/save-image`, PNG 매직 검사, review/ 만 허용).
- **순서 변경**: 사이드바 HTML5 DnD → `App.tiles` 재배열 → `tiles/manifest.json._order`. 파이썬·Node 내보내기 모두 `_order` 우선.
- 검수 데이터 구조 변경: `{status,good,request,ext}` → `{regions:[{x,y,w,h,text}], note}`. `migrateReview()` 가 구형 request/extNote 를 note 로 합침.
